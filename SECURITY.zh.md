# 安全策略
[English](SECURITY.md) | 简体中文

## 支持的版本

| 版本 | 是否支持 | 实测过的 dsh 线 |
| --- | --- | --- |
| 1.0.0 | 是 | `0.1.5-rc.2`、`0.1.6-alpha.2` |

本项目处于 1.0 之前，只有一条发布线。安全修复只进最新版。

## 报告漏洞

请在 <https://github.com/BOWLUNA/dsh-zcode-scribe/security/advisories/new>
开一个私有安全公告，或使用 issue 区的联系入口。
**在修复存在之前，请不要为任何可能被利用的问题开公开 issue。**

请附上 dsh 版本、操作系统，以及——如果有的话——一个能复现的记忆房间与请求路径。
**一条可复现的路径，比一段对路径的描述有价值得多。**

## 什么样的问题算漏洞

本插件存在的意义就是让三条属性成立，因此对其中任何一条的攻击都算安全报告，而不是普通缺陷：

1. **被收窄的写手。** 任何让「决定记什么」的那个组件够到它被记录为**不具备**的能力——
   网络、shell、MCP 服务、子代理、展示类与上传类工具——都是本项目能收到的最严重一类报告。
2. **路径安全。** 任何让 `scribe_recall` 读到记忆房间之外的文件、或让被禁的段被当成普通段解析的输入。
   `src/paths.mjs` 里的四道归一化，每一道都对应一种「没有它就会被利用」的攻击：
   Unicode 双向覆盖（U+202E 会把名字渲染成反序）、NTFS 备用数据流（`notes.md:hidden`）、
   尾部点与空格（Windows 会把 `foo.` 归一成 `foo`）、以及平台确实折叠大小写时的大小写折叠。
3. **沉默。** 模型没有被明确告知的截断、被拒的写入、或丢失的记忆。
   索引上限是**刻意**返回一条点名维度与上限的警告的；把它改得更安静，
   是安全属性的退化，不是外观改动。

## 威胁模型

记忆是攻击面。**OWASP ASI06「记忆与上下文投毒」已进入 2026 年 Agentic Top 10**，
而已发表的数字毫无悬念：记忆投毒载荷实测注入成功率约 **98%**、激活率约 **60%**，
而现有单轮注入过滤器对它的检出率是 **0%**。

难点不在于载荷有多精巧，而在于**合法的记忆写入与恶意的记忆注入是同一个操作**——
同一个文件、同一个写入调用。**只有意图不同，而意图不可观测。**
所以本插件不去检测意图：它**移除能力**，再约束剩下的。
`docs/ARCHITECTURE.md` 把每一条它点名的威胁都对应到一个具体机制。

本项目**不宣称**解决了记忆投毒。语义攻击无法被可靠检测，而效用与安全之间本来就有权衡。
它宣称的范围更窄、但可核查，`docs/ARCHITECTURE.md` §4.7 把这几条逐条写了出来。

## 范围外

以下不在范围内：任何以「攻击者已经控制 harness 进程、用户账号或本机」为前提的问题。
本插件没有网络面、没有自己的鉴权、也没有可被攻击的服务。
