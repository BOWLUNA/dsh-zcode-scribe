# 更新日志

本项目的显著变更。格式参照 [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/spec/v2.0.0.html)。

与 `dsh` 的兼容性写在 `package.json` 的 `engines.dsh` 里，并由
`tools/verify-version-consistency.mjs` 断言；某个版本的发布说明里列出的版本，
就是 CI 实际跑过的版本。

---

## [未发布]

### 新增

- 暂无。**写入是下一个里程碑**，而它卡在 `AGENTS.md` 所述的 M0 探针上：
  在做抽取之前，必须先证明「子代理的能力集真的能通过公开接缝被收窄」。
  如果证明不了，抽取功能**不上线**，而不是退化成「让主 agent 用全套工具去写记忆」。

---

## [0.1.0] — 2026-09-21

首个版本。**只读，且是刻意的。** 它的目的是先把三条安全属性**用代码和测试立起来**，
趁还没有任何功能需要靠牺牲它们来保住。

### 新增

- **`scribe_recall`**，一个只读工具。返回记忆索引、一份「主题文件名 + 其 frontmatter 里的
  `description`」的召回清单，以及可选的某一个主题正文。
  **主题正文永远不会被自动注入**——模型按需读取它真正需要的那个，这样更省，也更诚实。
- **记忆房间的路径规则**（`src/paths.mjs`），作用于该工具执行的每一次读取。
  比对之前先做四道归一化，每一道都对应真实攻击而非假想：
  剥离 Unicode 控制字符与双向字符（U+202E 会把名字渲染成反序）；
  在第一个 `:` 处截断，使 NTFS 备用数据流无法藏住一个被禁的名字；
  去掉尾部点与空格，因为 Windows 会把 `foo.` 归一成 `foo`；
  只在文件系统真的折叠大小写的平台上折叠。
- **相对根目录的段黑名单**，覆盖三类：安全边界（`.git`、`.ssh`、`.aws`、`.gnupg`、`hooks`）、
  宿主自有目录（`.dsh`、`skills`、`commands`、`agents`）、以及从来不是记忆的厂商状态目录
  （`node_modules`、`.vscode`、`.cargo` 等）。
  它比对的是**相对记忆根的路径**，因为房间本身合法地住在一个点开头的目录下。
- **索引上限**：200 行或 25000 字符，先到者为准——这是 Claude Code 调过并公开的那一对数字。
  超限时**截断并附上警告**，警告里点名维度、当前值与上限。理由很直接：
  一个以为自己写进去了、实际没写进去的 agent，比一个知道自己被截断的 agent 处境更糟。
  **静默截断永远不可接受。**
- **冻结的提示词快照**：在会话的提示词首次组装时捕获，之后不再变化。
  这既让提示词前缀可缓存，也阻止了一次召回去改写「造成它的那个会话」的指令。

### 说明

- **没有写入、没有抽取、没有子代理。** `narrowWriter` 与 `refuseSecrets` 作为配置项存在，
  并按它们的本来面目记录；把 `narrowWriter` 关掉时，它自己的注释会点名**失去的是哪条属性**。
- 所有工具名都带 `scribe_` 前缀，因为工具名在每个宿主里是全局的，重名是**启动硬失败**。
  本插件设计上要与之协作的 `dsh-memento` 注册的工具名正是 `memory` 与 `memory_recall`。
- 已在 dsh `0.1.5-rc.2`（桌面版）与 `0.1.6-alpha.2`（WSL 版）上验证。
  两条线的工具编写 DSL 有差异——其中一条会拒绝 `required: false`——
  这就是 CI 同时跑两者的原因。

[未发布]: https://github.com/BOWLUNA/dsh-zcode-scribe/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/BOWLUNA/dsh-zcode-scribe/releases/tag/v0.1.0
